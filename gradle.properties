org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Mod Properties
mod_version=1.1
maven_group=com.archivist

# Per-version overrides
deps.fabric_loader=[VERSIONED]
deps.fabric_api=[VERSIONED]
deps.loom_version=[VERSIONED]
version_range=[VERSIONED]

# Meteor Client (must be enabled to compile Meteor modules)
deps.meteor_enabled=true
deps.meteor=1.21.11-SNAPSHOT
